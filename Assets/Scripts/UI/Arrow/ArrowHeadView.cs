using UnityEngine;

namespace UI.Arrow
{
    public class ArrowHeadView : MonoBehaviour
    {
        private void Update()
        {
            //if (Physics.Raycast(transform.position, ))
        }
    }
}